setwd('c:/Users/asus/Downloads')
##
## input x as matrix
## set every solved value "a" as null
## changed every "mean" to "solve"
gogoCachematrix <- function(x = matrix(sample(1:500,7),9,8) {
    a <- NULL
    usb <- function(y) {
        x <<- y
        a <<- NULL
    }
    grab <- function() x
    usbgrab <- function(solve) a <<- inverse
    usbgrabgo <- function() s
    list(set = set,
         get = get,
         setmean = setmean,
         getmean = getmean)
}


## input x as matrix
## set "a" as null
## change "mean"to "solve"

cacheSolve <- function(x, ...) {
    ## Return a matrix that is the inverse of 'x'
    x <- x$getmean()
    if (!is.null(a)) {
        message("getting average data")
        return(a)
    }
    data <- x$get()
    a <- solve(data, ...)
    x$setmean(a)
    v
}
